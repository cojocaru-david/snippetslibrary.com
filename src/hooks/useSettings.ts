// Re-export useSettings from the SettingsContext for backward compatibility
// This allows existing code to continue working while benefiting from the global caching
export { useSettings } from "@/contexts/SettingsContext";
